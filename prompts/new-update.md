-User Client:

-in the login/register page, remove the gradient dark bg, replace it white modern white in default but can be also change to dark(not gradient) when clicked darkmode/light mode toggle.

-in the client user's catalog page, i want every cards can be clicked and show/view using modal with complete info specs and description.

-in the pc builder page, add also a darkmode theme and toggle button.

-Cashier:

-in cashier, change the POS page content in walk in sale remove the barcode, the pc builder of the client is just and option if the user wants to DIY build. Change the Walk in sale that also allows the other walk in customer buy other items like CPU or GPU only, display all my available products wih images in cards with search and filter that the cashier can click and added to the customers checkout payment (do the bussines logic there), use the reference: pos.html

-in POS dashboard kindly fix the buttons alignments.

-Super Admin:

-in admin's dashboard page content kindly redesign the card staticstics into a beautiful charts or graphs visualization except " Recent Sales" and "Low Stock Items"

-in admin's product content add also a view button per product that allows the admin to see, preview the product, with info, details

-in the admin's POS dashboard kindly fix the buttons alignments.

